# 掌心网站 · 交接说明

palmpoetry.org　更新于 2026-09-22

接手这个网站，先读这一页。读完你会知道：它由什么组成、怎么发布、数据在哪里、谁能改什么、哪里还有坑。

---

## 一、一分钟看懂

palmpoetry.org 是**一张手写的静态网页**加**一个 Supabase 数据库**。

- 网页：`上线包_掌心/index.html`，约 27 万字节的单文件，含 34 段内嵌脚本。另有 `covenant.html`（公约）、`fireflies.html`（萤火全本）、`privacy.html`、`thanks.html`、`404.html`。
- 数据：成员名片、作品、诗帖、雅集、上传的文件，全在 Supabase，由页面脚本运行时取回。**网页里没有内容，只有取内容的办法。**
- 托管：**Cloudflare**。GitHub 仓库推送 → Cloudflare 自动部署 → palmpoetry.org。
  域名解析到 172.64.80.1（Cloudflare 地址段），响应头 `server: cloudflare`。
  **Netlify 是旧方案，已弃用**——`thepalmpoetrysociety.netlify.app` 与 `beyondathousandmountains.netlify.app` 均已 404。仓库 README 里关于 Netlify 的说法是历史遗留，勿信。

没有构建步骤，没有框架，没有 node_modules。改 HTML 就是改网站。

---

## 二、怎么发布

```
1. 改 上线包_掌心/ 里的文件
2. 在仓库根目录跑：python 发布前自检.py        ← 必须全绿
3. GitHub Desktop → Commit → Push
4. Netlify 收到推送自动部署，约一分钟上线
```

**Cloudflare 不可退。** 它是这个域名的门房，停掉 palmpoetry.org 当场无法解析。它与已停用的论坛无关。

**Cloudflare 有缓存**：推完立刻打开可能还是旧版，等半分钟到一分钟再看，或在网址后加 `?v=2` 绕开。

### 发布前自检在查什么

文件是否被截断（未以 `</html>` 结尾）、是否残留「草稿 DRAFT」水印、有没有空的 `img src=""`、本地图片/链接是否缺失。

> 这个脚本本身曾经是坏的——它的 `main()` 在 2026-09-22 之前是截断的，一跑就 SyntaxError。一个专门检查截断的脚本自己被截断了，所以从提交那天起就没真正执行过。现在补好了。**改完一定要跑。**

### 另一个必查项：脚本块语法

`index.html` 里有 34 段内嵌脚本。改动 JS 后，把它们抽出来逐个 `node --check`：

```bash
python3 -c "
import io,re,os
s=io.open('上线包_掌心/index.html',encoding='utf-8').read()
bs=re.findall(r'<script(?![^>]*\bsrc=)(?![^>]*application/ld\+json)[^>]*>(.*?)</script>',s,re.S)
os.makedirs('/tmp/jc',exist_ok=True)
[io.open('/tmp/jc/b%03d.mjs'%i,'w',encoding='utf-8').write(b) for i,b in enumerate(bs)]"
for f in /tmp/jc/b*.mjs; do node --check "$f" || echo "✗ $f"; done
```

自检脚本不查 JS 语法。这一步救过一次——删旧代码多切了两行，留下孤儿代码，当场被查出来。

---

## 三、数据库（Supabase）

项目 `PalmPoetryClub`，域 `ubzykpglausstbsvijoc.supabase.co`。

### 表

| 表 | 用途 | 当前行数 |
|---|---|---|
| `members` | 成员名片：姓名、分组、专长、简介（中英）、照片、绑定邮箱 | 74 |
| `works` | 作品区。`kind` 三种值：`member` 成员作品 / `collection` 历年诗文汇 / `video` 掌心影音 | 51 |
| `work_revisions` | 作品每次改动的快照，可回溯谁在什么时候改了什么 | 69+ |
| `posts` | 诗海流风的诗帖 | 14 |
| `gatherings` | 雅集（日期、地点、封面、正文、参与者）—— **已建好，尚未接入界面** | 0 |
| `gathering_assets` | 雅集的照片与附件，一条雅集挂任意多条 | 0 |
| `admins` | 谁能管哪一块 | 5 |
| `policy_backup` | 改权限策略前留的底，回退时照抄 | 1 |
| `view_counts` | **已废弃**，不要再读写 | — |

### 视图

`posts_public` —— 诗帖的对外视图。比 `posts` 少一列 `author_email`，多一个布尔值 `mine`（这条是不是当前登录者发的）。**前端只读这个视图**，靠 `mine` 决定要不要显示「修改／撤下」。

### 关键函数

`is_admin(want)` 判断当前登录者是不是某一块的管理员；`is_bound_member()` 判断邮箱是否已绑定名录；`my_member()` / `my_work()` 取本人的名录行与作品行；`backup_json()` 一次导出整库 JSON（只读）。

### 权限模型 —— 这一段最要紧

**邮箱不能对外泄露。** 做法不是靠 RLS，而是靠**按列授权**：

- `members`、`works`、`posts` 都是**逐列**授予读取权限的，`owner_email` / `author_email` **不在授予之列**。未登录访客请求这些列直接 401。
- `admins` 表**任何前台角色都读不到**，前端只能通过 `is_admin()` 问一句是／否，拿不到名单和邮箱。

**这带来一个必须记住的陷阱：**

> **往这三张表加新列，必须单独授权，否则前台读不到。**
> ```sql
> grant select (新列名) on public.works to anon, authenticated;
> ```
> 2026-09-22 加 `works.body` 时就漏了这一步，结果**未登录访客整个作品区加载失败**，页面只剩静态兜底卡片。当天发现并补上。

### 谁能做什么

| 操作 | 谁 |
|---|---|
| 改自己的名片 | 本人（邮箱匹配） |
| 传／改自己的作品 | 本人，且已绑定名录 |
| 传历年诗文汇、雅集 | `admins` 里 scope 含 `collection` / `gathering` 的人 |
| 传掌心影音 | scope 含 `video` |
| 发诗帖 | 任何已绑定名录的成员 |
| 改／撤自己的诗帖 | 本人 |
| 撤任何人的诗帖 | scope 含 `post` |
| **代别人改作品卡** | **谁都不行**（2026-09-22 明确决定） |

当前 `admins`：晓鸣 `{work}`、飞鸿 `{work,post}`、文刃 `{post}`、海边 `{collection,gathering}`、挑灯看剑 `{video}`。

加人只要加一行，**不要再把邮箱写死进权限策略**——旧做法就是写死的，每加一个人都得改策略。

### 存储桶

`works`（作品文件与封面）、`member-photos`（名片照片）、`gallery`（雅集相册，2026-09-22 新建），都是公开读。

---

## 四、2026-09-22 这次改版做了什么

1. **雅集相册上限 30 → 200 张**，并修掉了一个静默截断的 bug：原先超过 30 张的照片被悄悄丢弃，界面还显示"已保存"。这是海边多年传不上照片的真正原因。上传时现在逐张显示进度。
2. **作品卡新增正文字段**（`works.body` / `body_en`）。散文与赋不必再只能传文件或挂外链。填了正文的卡片，点击改为打开通读窗。
3. **论坛停用，改为站内「诗海流风 Poems on the Tide」**。原 Discourse 论坛注册用户只有 6 人（名录 74 人），帖子几乎零回复，独立账号是致命伤。14 帖连原作者与原日期迁入 `posts` 表。**全站与仓库已无任何 discourse 链接。**
4. **57 个「尚待上传」空卡不再公开陈列**。空间照常保留，本人登录后只看到自己那一格。
5. **站内搜索覆盖正文**：人名、作品名之外，作品正文与诗帖正文也能搜到，带上下文片段，点击直接开通读窗。
6. **英文并行**：新增 31 段英文并行文字（三扇门、文存、成员与作品、诗海流风、加入表单、登录说明），并把旧英文理齐——人名口径统一、去翻译腔、被动句改成「我们」。
7. **邮箱防泄露**：`posts` / `admins` 的授权收紧到按列，`posts_public` 视图上线。
8. 补好了 `发布前自检.py`。

---

## 五、还没做完的

- **`gatherings` / `gathering_assets` 两张表建好了但界面还没接。** 历年诗文汇目前仍用老办法：把日期、简介、正文、相册网址打包成 JSON 塞进 `works.note` 一个字段里。能用，但笨。接新表是下一步。
- **诗海流风的欢迎帖正文还是 `Hello World!`**，Discourse 建站样板，迁过来没改。等一段真正的欢迎语。
- ~~`上线包_千山独行/` 缺 4 张图~~ **2026-09-22 已补齐**：Leon 提供原件，`网站图片/许之远/` 四张就位（1958 蔣經國合影原扫描是躺着的，已转正 90°）。同日修了自检脚本两处：站点绝对路径 `/beyondathousandmountains/…` 不再误报为缺失文件；结尾提示由 Netlify 改为 Cloudflare。现在两个上线包 13 个文件**真·全绿**。
- **《掌心征稿体例》已成稿未上线**，见仓库里的 `掌心征稿体例.md`，四个数字待定（现代诗上限、古典诗词上限、散文字数、答复时限），投稿邮箱是否单设也未定。

## 六、一个建议修但没敢动的

**任何已登录的成员，都能读到全部 74 人的邮箱。**

`owner_email` 对未登录访客是关闭的，但对 `authenticated` 角色是开放的——这是改版前就有的状况，不是这次引入的。站点自己的代码用 `my_member()` / `my_work()` 取本人数据，理论上不需要这个权限。

建议的修法：

```sql
revoke select (owner_email) on public.members from authenticated;
revoke select (owner_email) on public.works   from authenticated;
```

**改之前务必逐项回归测试**：登录、改名片、传作品、传诗文汇、发诗帖、空卡入口。有一处依赖它就会当场坏掉，所以没有在这次一并改。

---

## 七、备份与回退

**动数据库之前，先导出留底。** Supabase 后台 → Table Editor → 打开 `members` / `works` / `work_revisions` / `posts` → 右上角 Export → Download as CSV。或直接 `select backup_json();`，一次导出整库 JSON（这个函数只读，不写）。

导出后**核行数**，不要只看文件在不在。

**改权限策略前**，先把原策略存进 `policy_backup` 表：

```sql
insert into public.policy_backup(tablename, policyname, cmd, qual, with_check)
select tablename, policyname, cmd, qual, with_check
from pg_policies where schemaname='public' and tablename='<表>' and policyname='<策略名>';
```

网页回退就是 git revert，Netlify 会自动重新部署。

---

## 八、呈现规矩

`上线包_掌心/MAINTENANCE.md` 是这个站的呈现铁律——社在学园之上、各组按真实更新时间独立排序、不排名次不加状态说明、`view_counts` 已废弃、英文名必须是 `The Palm Poetry Society`（带 The）、文刃《千山独行》那张卡永远排在主区最后、空的作品空间保留但不公开陈列、诗海流风不得再链回 Discourse。

**改任何东西之前先读它。** 它是一路踩过的坑攒出来的，不是装饰。
